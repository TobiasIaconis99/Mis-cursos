1. ¿Que es un objeto Jquery?


2. ¿Por que esto no es posible?
    function cambiarTexto(){
        let texto = document.getElementById("texto");
        texto.text("texto cambiado");
    }
    o esto
    function cambiarTexto(){
        let texto = $(#texto);
        texto.InnerText = "Texto cambiado";
    }

3. Todas las funciones de un objeto Jquery 

